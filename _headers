# Cloudflare Pages headers file
# Uses HTTP caching with revalidation - browser checks if file changed before using cache

# HTML - always revalidate
/*.html
  Cache-Control: no-cache

/index.html
  Cache-Control: no-cache

# JS files - always revalidate (304 if unchanged, new file if changed)
/js/*
  Cache-Control: no-cache
